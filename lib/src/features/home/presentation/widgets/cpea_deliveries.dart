import 'package:flutter/material.dart';

class CPEADeliveries extends StatelessWidget {
  const CPEADeliveries({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      margin: EdgeInsets.symmetric(horizontal: 24),
      child: Column(
        children: [],
      ),
    );
  }
}
