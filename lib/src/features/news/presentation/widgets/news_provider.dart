import 'package:flutter/material.dart';

class NewsProvider extends StatelessWidget {
  const NewsProvider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
