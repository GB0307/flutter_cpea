const cardBorderRadius = 6.0;
const chipBorderRadius = 60.0;
const cardMargin = halfPadding;

const padding = 8.0;
const doublePadding = padding * 2;
const triplePadding = padding * 3;
const halfPadding = padding / 2;
const quarterPadding = padding / 4;
const horizontalPadding = doublePadding;

const elevation1 = 1.0;
const elevation2 = 2.0;
const elevation3 = 3.0;

const defaultCardButtonSize = 52.0;
const defaultCardTileHeight = 75.0;
const defaultCardTileThreeLineHeight = 90.0;
const defaultCardTileDenseHeight = 48.0;

const defaultLeadingWidth = 64.0;
